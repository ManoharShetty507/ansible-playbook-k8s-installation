---
- name: kubeadm join master
  hosts:
    - master1
    - master2
  tasks:
    - name: Join cluster as master node
      become: true
      shell: bash /tmp/kubeadm-join-command.sh

    - name: Join cluster as master node
      become: yes
      command: bash /tmp/kubeadm-join-command.sh

